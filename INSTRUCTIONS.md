# SQL Exam Tutor - Gamified Edition

ยินดีด้วยครับ! ตอนนี้โปรเจกต์ของคุณเชื่อมต่อกับ **Supabase** เรียบร้อยแล้ว พร้อมระบบ Gamification เต็มรูปแบบ 🎮

## 📦 ฟีเจอร์ที่เพิ่มเข้ามา
1.  **XP & Level System**: เก็บค่าประสบการณ์และเลเวล (Novice -> Grandmaster)
2.  **Achievements**: เหรียญรางวัลปลดล็อกตามเงื่อนไข (เช่น ทำโจทย์เร็ว, มาเรียนต่อเนื่อง)
3.  **Tutor Mode (Time Attack)**: มินิเกมจับเวลา "Mini Drill" ที่ให้ XP จริง
4.  **Cloud Save**: ข้อมูลผู้ใช้ซิงค์กับ Supabase (เข้าใช้งานจากที่ไหนก็ได้)

## 🚀 วิธีการรันโปรเจกต์
1.  เปิด Terminal ใน VS Code
2.  พิมพ์คำสั่ง:
    ```bash
    npm run dev
    ```
3.  เปิด Browser ไปที่ `http://localhost:3000`

## 🧪 วิธีทดสอบระบบ
1.  **Login**: ใส่ชื่อของคุณ (ระบบจะสร้าง ID ให้เองอัตโนมัติ)
2.  **Check Level**: ดูแถบ XP ที่เมนู Sidebar ด้านซ้ายล่าง
3.  **Play Game**: ไปที่เมนู **"โหมดติวเข้ม"** -> เล่น **Time Attack Mini Drill**
    -   ตอบถูกได้คะแนนและ XP
    -   ถ้าทำเวลาดี จะได้ Achievement "ปีศาจความเร็ว" (Speed Demon) 🔥

## 🛠️ ข้อมูลทางเทคนิค (สำหรับนักพัฒนา)
-   **Database**: PostgreSQL (Supabase)
-   **ORM/Query**: Supabase Client (`@supabase/supabase-js`)
-   **Schema**: ดูไฟล์ `data/gamification.sql`
-   **API Endpoints**:
    -   `POST /api/user/login`: สร้างหรือดึงข้อมูลผู้ใช้
    -   `POST /api/user/sync`: บันทึก XP และ Achievements ล่าสุด

ขอให้สนุกกับการเรียนรู้ SQL นะครับ! 🚀
